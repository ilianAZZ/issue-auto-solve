services:
  issue-auto-solve:
    build: .
    container_name: issue-auto-solve
    restart: unless-stopped
    env_file: .env
    environment:
      CONFIG_FILE: /app/config/issue-auto-solve.yml
      STATE_DIR: /app/state
      WORKSPACE_DIR: /app/workspaces
      LOG_DIR: /app/logs
      # The Docker daemon resolves bind mounts on the host, so agent containers
      # must be given host paths, not the paths seen from inside this container.
      HOST_STATE_DIR: ${PWD}/state
      HOST_WORKSPACE_DIR: ${PWD}/workspaces
    # Loopback by default: the dashboard is token-protected but has no TLS. Reach it over
    # an SSH tunnel, or set BIND=0.0.0.0 once a reverse proxy terminates TLS in front.
    ports:
      - "${BIND:-127.0.0.1}:${PORT:-8420}:8420"
    volumes:
      - ./config:/app/config:ro
      - ./secrets:/secrets:ro
      - ./state:/app/state
      - ./workspaces:/app/workspaces
      - ./logs:/app/logs
      - /var/run/docker.sock:/var/run/docker.sock
